
  <%- include('includes/header') %>
  
  <div class="container py-md-5 container--narrow">
    <div class="text-center">
      <h2>Hello <strong><%= user.username %></strong>, you are <strong><%= user.role  %></strong>.</h2>
      
      <% if (user.role == "driver") { %>
        <a class="btn btn-sm btn-success mr-2" href="/create-truck">Create Truck</a>
      <% } else { %>
        <a class="btn btn-sm btn-success mr-2" href="/create-load">Create Load</a>
      <% } %>

      <p class="lead text-muted">Your feed displays the latest posts from the people you follow. If you don&rsquo;t have any friends to follow that&rsquo;s okay; you can use the &ldquo;Search&rdquo; feature in the top menu bar to find content written by people with similar interests and then follow them.</p>
    </div>  
  </div>
  
  <%- include('includes/footer') %>